pandas~=1.4.4
seaborn~=0.11.2
scikit-learn~=1.1.3
matplotlib~=3.5.3
mne~=0.23.0
keras_tuner~=1.3.0
pydot~=1.4.2
graphviz~=0.20.1
# and any other Python packages you might need
# Queste sono le sole librerie aggiuntive che servono per "TesiLAMPO_PierMOD" rispetto a quelle gia' contenute nel
# container TF versione 2.12.0. Le versioni specificate sono quelle che ho usato correttamente su Python3.9.
# Se usiamo "==" per specificare la versione, intendiamo dire che vogliamo esattamente quella --> eventuali librerie gia'
# installate verranno portate a quella versione (anche con downgrade, se necessario). Utilizzando "~=", invece, intendiamo
# selezionare l'ultima versione del pacchetto: nello specifico chiediamo che la versione sia "greater than or equal to".
# Quindi se gia' installata una versione compatibile con questa richiesta, verra' mantenuta quella versione; se il
# pacchetto, invece, non e' presente o e' presente una versione precedente a quella dichiarata, allora verra' installata
# l'ultima versione disponibile.